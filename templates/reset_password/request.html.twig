{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublié | {{ parent() }}{% endblock %}

{% block body %}
    {% for flashError in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}
    <div>
        <h2>Réinitialiser mot de passe</h2>

        {{ form_start(requestForm) }}
        {{ form_row(requestForm.email) }}
        <p>
            Veuillez rentrer votre mail pour recevoir un lien de réinitialisation de mot de passe.
        </p>
        <br>
        <button class="btn btn-primary">Envoyer email</button>
        {{ form_end(requestForm) }}
    </div>
{% endblock %}
